# 两 Driver 现场部署记录（2026-09-23）

本记录补充同日离线记录，不替代真机运动验收。用户明确确认现场无人使用，授权停止项目并部署两个 Driver，保留 Core / ActuCore。

## 已执行

- 通过普通 Canvas API 停止项目，读回 idle；旧遥操、执行器、servo、VLA 空闲，旧遥操无控制权及输出。
- 切换天轶 Driver 至 `sha256:941c3bb99ad6e6641ca7e9715ff941883145e7c3679c0de1369dd83c06c50b4d`，关闭旧四卡入口、保留 Shadow、关闭开机自检动作。新 `teleop_control` 注册、空闲、未绑定、无操作者会话。
- 部署独立 PICO Driver `sha256:94768a952f6dd0f5c041e0ca67e7d72c40405635fa78b76ca7824604a1c5a806`，普通 MCP 注册成功。原证书与配对记录仅复制到本任务私有状态目录；不迁移旧齿轮配置，不把新 App 填为已安装。
- 切换完成时核对 Core、ActuCore、Perception 的容器 ID、配置、启动时间与既有 Compose 哈希均未变。没有启动项目或发送真实动作。
- 部署脚本隔离检查 9 项通过；画布增量脚本验证保留原卡、连线、执行边和视图，拒绝重复添加。

## 并发更新与当前未完成项

添加两张卡前重新查询，发现新的 Canvas 编辑锁；脚本在写画布前退出，原 14 张业务卡未修改。

约 20:41 又观察到另一轮服务重建：天轶 Driver 已被替换为普通 `release.260921.f630d24`，ActuCore 已变为 `release.260920.3f0cae9-jetson-jp6.1`；Core 替代容器处于 Created、尚未启动，Canvas 端口拒绝连接。该重建不在本次部署脚本范围内；尚未确定发起者。PICO Driver 仍运行。本次没有反向覆盖这轮更新。

因此不能宣称两卡生产页面、现场端到端或真机验收通过。已请求用户确认整套更新结束后再恢复两个 Driver；恢复应读取最新服务及配置，不套用旧容器快照，也不回滚 Core / ActuCore。

## 只读 ROS 检查

6 秒双臂状态订阅收到 2984 条消息，约 497.3 Hz，最大间隔 29.22 ms，末次年龄 1.59 ms；14 个关节错误码均为零，末次最大绝对速度约 0.0009613 rad/s。观察到 1 个状态发布者、2 个位置命令发布者和 59 个节点。发布者数量不等于正在执行动作，不能替代控制权或跟随证据。

机上证据位于本任务 `updates/two-driver-20260923/evidence/deployment-{preview,created,result}.json`；准备、切换及画布脚本保留在同一本事项目录。后续仍需完成两卡齿轮、实体 App 和真实动作验收，再申请精确提交 BOT review。
