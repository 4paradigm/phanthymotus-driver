# 天轶遥操 PR 交付

## 范围

按用户要求提交最新遥操实现，分别向 PhanthyMotus 主仓与 Driver 仓创建 Draft PR。使用独立工作树固定快照，不修改原开发树、Agent Core 或现场服务，不触发硬件动作。主仓包含 ActuCore/PICO/构建/回放；Driver 仓包含天轶执行器及测试文档。Canvas 专用面板与 Core 管理代理留在原开发树，未纳入本次。

## 过程与发布边界

1. 固定两个仓库的当前工作树代码，保留来源及许可证，不带入原始录制、凭据或现场私有 journal。
2. 复跑本地测试，保留环境失败和代码/测试不一致，不把 Draft 标成可合并。
3. 同仓代码和文档同次提交，推送个人 fork 并建立互相引用的 Draft PR，不自动合并或部署。

## 已有物理证据（引用原事项记录，本次不重测）

原事项 r58/r15 记录独立执行与 10 秒完整输入回放均结束、双臂实测移动且停止释放确认；仍有约 1.206 秒发布空窗，不能声称连续跟随通过。后续 r62 收臂记录约 2.03 秒结束、最大零位偏差约 0.00396 rad、停止并释放；通过服务端同一 finish 入口，不等于实体按钮验收。

未完成：实体结束/停止按钮、15 分钟连续遥操及至少 10 轮恢复。跟随误差只报告，不新增通过门槛。机上证据、镜像构建和物理结果不冒充本次 PR 的独立测试结果。

## 本次验证

2026-09-22 在独立 PR 工作树运行，未访问硬件：

- ActuCore 天轶、操作会话、RTC 传输及显示专项：259 passed、1 skipped。跳过的原始录制用例需要未随 PR 发布的私有数据。
- ActuCore 全目录（提供 TIANYI_DRIVER_SOURCE）：537 passed、51 failed、1 skipped。50 项 G1 失败缺少独立 CasADi ABI 环境；1 项 Core 管理头测试依赖本次明确排除的 Agent Core 补丁，不宣称完整面板可用。此前未设置 Driver 路径的首次运行报 62 个收集/夹具错误，补齐环境后消失。
- Driver 全目录（PYTHONPATH=x-humanoid/tianyi2.0）：154 passed、9 failed。3 项旧 servo 测试的中断语义/ROS stub 不匹配，5 项最新输入测试缺少新增追踪字段，1 项厂商写入测试替身缺少会话字段。本次仅提交快照，不修改行为或掩盖失败。
- PICO 原生 host suite 通过；capture_wire_test 因未设置 NLOHMANN_JSON_INCLUDE 跳过。本次未重新构建 APK 或机器人镜像。
- 已核对主仓暂存中 Agent Core 路径为空；原工作树与机器人部署保持不变。默认保持 Draft，以上失败及现场长期验收均是合并前待办。

可复现命令见遥操 README。README 已重组为当前契约并明确 Core 集成不在本次范围；Driver 带日期段落保留历史属性。未发布现场录制、密钥、租约 journal、个人飞书消息及私有验收日志。
