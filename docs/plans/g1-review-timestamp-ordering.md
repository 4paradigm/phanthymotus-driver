# G1 Bot 审查：时间戳顺序修复

针对 PR #154 评论 5750753677：修复归一化后的 LiDAR/IMU 时间戳重复或倒序
时被递增伪造的问题。异常返回 None，由现有回调丢弃路径处理，不推进已接受
时间戳；分别增加 cloud/imu_non_increasing_timestamps 计数。新鲜递增样本
仍能继续发布，不修改时钟域映射、安全阈值或其他机型。

验证两种流的正常、重复、倒序、后续恢复分支，运行 G1 全量测试及 service
检查。README 补充重复/倒序归一化时间的丢弃与计数语义；Dockerfile 仅补充镜像
增长说明，不新增依赖。修复提交推送后申请 Bot review，直到最新 head 明确
通过；不部署机器人、不把构建通过视为代码审查通过。
