# 天轶遥操执行链适配与审查计划

## 目标与范围

将连续遥操的机器人执行入口接入现有 Tianyi 2.0 Driver。PICO、配对、动作映射、IK、碰撞/可达性显示、操作者会话及收臂轨迹由普通 ActuCore 内的 teleop 卡片承担。Driver 不包含 PICO 客户端或 IK，也不新增独立 ActuCore 服务。

来源开发树基于 b1cbc93e3b1e15f67920cd4e4218110e2749de3b。本审查树从实时主线 19729470df853926ba170838a1e158c17ae1392e 移植 23 个显式路径，无冲突，保留主线新增的导航停止修复。变更限定 Tianyi bundle 入口、原异步动作取消语义、连续执行器、构建、相关测试和文档，不改其他机型或 Agent Core。

## 已实现

- 默认关闭的 teleop_executor，回环 MCP 管理与本机 domain-42 连续目标，厂商 domain-0 输出复用现有 ArmPlugin/HandPlugin。
- 共享执行权覆盖旧动作、轨迹、手势、手部、头腰、底盘、导航和 servo；取消超时继续保留忙状态，不静默抢占。
- 50 Hz 最新目标、签名/会话/序号/原始时效验证、限位限速和有限领先；旧会话报文不干扰新会话。
- 后续新鲜反馈确认保持和释放；短暂时效缺口及 IK recoverable_hold 同会话恢复，显式暂停/真正故障采用完整恢复。
- 幂等管理请求与取消，防止成功回复丢失后 lease 旋转或迟到请求意外接管。
- 独立安全反馈调度、有界让出、每次准备采集固定体位基准；双臂-only 不要求手部输出或手部反馈，却仍报告其状态。
- 首次限时验收与显式持续操作两种准备方式；准备不等于获取执行权，不伪填普通验收记录。
- 有界内存四段追踪与厂商发布诊断；不把发布成功等同于实际到位。
- 构建编译实际消息依赖并校验入口导入，默认功能保持关闭。

## 验证与提交整理

2026-09-22 整理时，已部署 Driver r16 的本地发布包与当前 motion_stream.py / teleop_executor.py 字节一致；本次未访问机器人验证运行态。此前同会话现场记录已确认双臂跟随、两次结束收臂并释放控制权；用户随后确认效果良好。该证据不扩展为手部、长期运行或进程崩溃保持全部通过。

本次离线审查覆盖状态机、首次准备、短暂续接、最新 socket 输入、生命周期、管理恢复、厂商消息转换、追踪、ROS 调度及现有相关 Driver 回归，定向 218 项通过。早期测试使用 __new__ 跳过初始化，新增 trace 后有 6 项缺字段失败；改为真实 TeleopExecutor/MotionGate 初始化，并增加独立模块加载以消除收集顺序依赖。构建脚本已通过 bash 语法、错误参数及隔离假 Docker 的真实运行，校验上下文、固定基础镜像参数和临时目录清理；本次未执行实际镜像构建。

来源开发树的 Tianyi 全目录与相关仓根回归共 254 通过 / 3 失败。test_servo.py 的 3 个既有失败涉及旧 interrupt hook / confirm_motion 预期与缺少 MotorStatusMsg stub；已在未修改 HEAD 的隔离 git archive 复现相同 3 失败 / 22 通过。本次不修改无关 servo 语义或掩盖失败。

移植到上述新主线后的扩展回归为 250 通过 / 7 失败，全部 7 项在未修改的新主线隔离副本复现：前述 3 项、ControlSink 时钟改动后旧 servo 假时钟的 3 项，以及 Adam Dockerfile 测试将测试模块当作生产依赖的 1 项。遥操定向回归 176 项通过；这些上游失败不伪装成当前变更通过，也不扩大范围修改其他模块。

已有 Draft PR #321 承载此前快照；本次只准备最新主线上的修复增量，沿用既有 PR，不重复创建。原开发树和现场服务均保留；已有 PR 工作树按用户授权合入主线并同步修复，代码与文档同次提交。

提交只包含公开源码、测试和可复用文档。原现场调试流水及内部地址、个人目录、消息 ID 已私存，不随公共 PR 发布。README、执行契约和部署说明按最终普通 ActuCore 架构同步，旧命令和独立实例方案不再作为操作入口。

## 架构后续事项

1. 当前 motion-target 协议与仓库已有 motus.control/1 ControlSink 并存。此轮先保留已验证行为和生产边界；后续独立评审哪些时效、仲裁、诊断语义可复用，避免直接替换造成停止/恢复回归。
2. 旧动作插件与遥操共享同一 Driver Python 进程，反馈调度通过专用执行器和有界让出缓解。需持续观察实际负载；不把一次低延迟样本当成确定性实时保证。
3. 停止确认采用订阅到达时间；厂商消息到达与硬件采样新鲜度不能混同。Driver 崩溃后的有限剩余目标需独立物理验收。
4. 持续操作准备、短时首次验收和普通 Live 验收是三种不同依据，后续应在卡片中清楚展示，避免现场临时模式长期隐式化。
5. 收臂是 ActuCore 的显式操作者动作；Driver 不在断线/暂停时自行回零。未来通用执行协议整合必须保持这个责任边界。

## 下一步

- 完成本分支范围化提交与 bot review，按实际评论修正；不将 bot 通过写作硬件验收。
- 天轶目前承担接待任务，不再访问或调试；仅离线继续工作。
- 北京 G1 的卡片展示/部署属于主仓独立工作，不复用天轶模型、14 关节映射或停止证据。
- 手部验收、长时运行和崩溃边界保留为未完成项，后续需新的设备窗口及对应授权。
