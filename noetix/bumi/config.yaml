mcp_port: 15704
ros_namespace: ""   # 留空则自动使用 hostname

plugins:
  state:
    enabled: true
  loco:
    enabled: true
  mic:
    enabled: true
  speaker:
    enabled: true
  camera:
    enabled: true
  media_system:
    enabled: true
  media_config:
    enabled: true
  wakeup_words:
    enabled: true
  audio_playback:
    enabled: true
  video:
    enabled: true
  low_cmd:
    enabled: false   # opt-in: requires LowController subprocess
  rl_policy:
    enabled: false   # opt-in: requires onnxruntime + ONNX model
