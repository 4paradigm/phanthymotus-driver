mcp_port: 15704
ros_namespace: ""   # 留空则自动使用 hostname

plugins:
  state:
    enabled: true
  loco:
    enabled: true
    # Persistent recording ID/name directory. The deployment mounts
    # /opt/phanthy-motus/data from the robot host, so it survives container and robot restarts.
    recording_registry_path: /opt/phanthy-motus/data/noetix/bumi/action_recordings.json
  mic:
    enabled: true
  speaker:
    enabled: true
  camera:
    enabled: true
  motion_state:
    enabled: true
    # 2 Hz: sufficient for Agent-facing motion telemetry without excessive DDS reads.
    poll_interval_s: 0.5
    # Activity classification only; this is not a motor safety or command limit.
    activity_velocity_threshold: 0.15
