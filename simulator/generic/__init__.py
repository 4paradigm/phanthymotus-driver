"""Generic embodiment simulator bundle.

Impersonates any embodiment (wheeled / quadruped / humanoid / arm) described by
`config.yaml`, exposing the common sensor and actuator cards over MCP so the
Agent Core canvas cannot tell it from a real driver.
"""
