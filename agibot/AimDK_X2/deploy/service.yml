agibot-x2:
  image: __IMAGE__
  container_name: embodied-agibot-x2
  network_mode: host
  ipc: host
  pid: host
  privileged: true
  volumes:
    - /dev:/dev
    - /opt/phanthy-motus/data:/opt/phanthy-motus/data
    - /opt/phanthy-motus/dds-local.xml:/opt/phanthy-motus/dds-local.xml:ro
  environment:
    - ROBOT_DOMAIN_ID=0
    - RMW_IMPLEMENTATION=rmw_fastrtps_cpp
    - NETWORK_INTERFACE=develop0
    - ROBOT_FASTRTPS_DEFAULT_PROFILES_FILE=/work/agibot/AimDK_X2/resource/fastdds_develop0.xml
    - CORE_FASTRTPS_DEFAULT_PROFILES_FILE=/opt/phanthy-motus/dds-local.xml
    - PYTHONUNBUFFERED=1
  logging:
    driver: local
    options:
      max-size: "10m"
      max-file: "3"
  restart: unless-stopped
